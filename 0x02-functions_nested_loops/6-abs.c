#include "main.h"
/**
 *_abs - Check Main
 * @r: An absolute value of the nuber r
 * This pr returns absolute value of a number
 * Return: Absolut value of number r
 */
int _abs(int r)
{
	if (r <= 0)
	return (r * -1);
	else
		return (r);
}

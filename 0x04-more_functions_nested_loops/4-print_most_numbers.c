#include "main.h"
/**
 * print_most_numbers - prints the numbers interleaved
 *
 * Return: numbers
 */
void print_most_numbers(void)
{
	int num;

	for (num = 48 ; num <= 57 ; num++)
	{
		if (numb != 50 && numb != 52)
	
		_putchar(num);
	}
	_putchar('\n');
}

#include "main.h"
/**
 * mul - multiplies two integers
 * @x: multiplying
 * @y: multiplier
 *
 * Return: the result of mutiplication
 */
int mul(int x, int y)
{
	int rslt;

	rslt = x * y;
	return (rslt);
}
